import type {Metadata} from 'next';import {normalizeTelHref,store} from '@/lib/store';export const metadata:Metadata={title:'Kontakt',alternates:{canonical:'/contact'}};export default function Page(){return <main className="mx-auto max-w-4xl px-4 py-12 text-slate-700"><h1 className="text-4xl font-bold text-primary">Kontakt</h1><div className="mt-6 space-y-3 rounded-3xl bg-slate-50 p-6"><p><strong>Telefon:</strong> <a className="text-primary underline" href={`tel:${normalizeTelHref(store.phone)}`}>{store.phone}</a></p><p><strong>E-pošta:</strong> <a className="text-primary underline" href={`mailto:${store.email}`}>{store.email}</a></p><p><strong>Radno vreme:</strong> {store.businessHours}</p><p>{store.serviceArea}</p></div></main>}
